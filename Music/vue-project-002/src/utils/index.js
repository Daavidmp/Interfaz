export function getPlaceholderSrc (size, textColor, backColor){
    return `https://placeholdit.com/${size}/${textColor}/${backColor}`
}

/*export function getPlaceholderSrc (img){
    return `https://placeholdit.com/${img.size}/${img.textColor}/${img.backColor}`
}*/

/*export function getPlaceholderSrc (img){
    const { size, textColor, backColor} = img

    const size = img.size
    const textColor = img.textColor
    const backColor = img.backColor
    return `https://placeholdit.com/${size}/${textColor}/${backColor}`
}*/